# Projet =======================================================================

cmake_minimum_required (VERSION 3.0)
project (Fractal)

# Structure ====================================================================

# Sources
set (SRC_PATH src/)
add_subdirectory (${SRC_PATH})
